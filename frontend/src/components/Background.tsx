/**
 * The background is intentionally nothing — just the paper (bone/ink) surface.
 * Swiss-minimal design leans on negative space and hairlines, not decorative
 * gradient blobs. Kept as a component so callers don't need to change.
 */
export function Background() {
  return null;
}
